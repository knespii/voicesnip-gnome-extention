# VoiceSnip System Requirements - CUDA Profile
# For Debian/Ubuntu systems (apt packages)

# Core system packages
python3
python3-pip
python3-venv
python3-dev
python3-tk
libportaudio2
portaudio19-dev

# Text insertion (display server detected at runtime, both stacks installed)
# X11:
xdotool
# Wayland (uinput-based typing + clipboard paste):
ydotool
ydotoold
wl-clipboard

# On-screen overlay (GTK3; VoiceSnip degrades silently without it)
python3-gi
gir1.2-gtk-3.0

git
pkg-config

# NVIDIA GPU drivers
# NOTE: nvidia-cudnn and nvidia-cublas are installed via pip (nvidia-cudnn-cu12, nvidia-cublas-cu12)
# User must have nvidia-driver installed (verify with nvidia-smi)
# Install with: sudo apt install nvidia-driver-XXX
# Check available versions: apt search nvidia-driver
